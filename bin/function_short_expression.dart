int numbers (int first, int second){
  return(first + second);
}

// int numbers (int first, int second) => first + second;

void main(){
  var total = numbers(10,10);
  print(total);

  print(numbers(10,5));
}